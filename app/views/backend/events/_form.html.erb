  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this team from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


<div class="row">
	<div class="c12"><%= f.label :name %><%= f.text_field :name, :placeholder => 'Name' %></div>
</div>
<div class="row">
	<div class="c12"><%= f.label :description %><%= f.text_area :description, :placeholder => 'Description' %></div>
</div>

<div class="row">
	<div class="c6"><%= f.label :date_start, :style => 'display: block;' %><%= f.datetime_select :date_start, {:minute_step => 15}, { class: "form-datetime" }  %></div>
	<div class="c6"><%= f.label :date_end, :style => 'display: block;' %><%= f.datetime_select :date_end, {:minute_step => 15}, { class: "form-datetime" } %></div>
</div>

<div class="row">
	<div class="c6"><%= f.label :bonus, 'Bonus Points' %><%= f.number_field :bonus, :placeholder => 'Bonus Points' %></div>
	<div class="c6"><%= f.label :user_id, 'Hosted By' %><%= f.select :user_id, @hosts, :prompt => "select a host" %></div>
</div>

