<% if @team.errors.any? %>
<div id="error_explanation">
  <h6><%= pluralize(@team.errors.count, "error") %> prohibited this team from being saved:</h6>

  <ul>
  <% @team.errors.full_messages.each do |message| %>
    <li><%= message %></li>
  <% end %>
  </ul>
</div>
<% end %>

<div class="row">
	<div class="c12"><%= f.text_field :name, :placeholder => 'Team Name' %></div>
	<div class="c12"><%= f.text_area :description, :placeholder => 'Please provide a description.' %></div>
</div>
